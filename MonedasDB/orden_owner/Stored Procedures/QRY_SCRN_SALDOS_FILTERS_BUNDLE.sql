CREATE PROCEDURE [orden_owner].[QRY_SCRN_SALDOS_FILTERS_BUNDLE]
AS
BEGIN
	SET NOCOUNT ON;
	--SELECT 0 as Orden, 'Moneda' as NombreCampo
	--Order by Orden
 
	--SELECT Descripcion as NombreMoneda, IdMoneda as IdMoneda FROM shared_owner.Monedas
	
END